# Client Environment
# Copy to .env for local dev

# API base URL. Default: Vite proxy üzerinden /api → server:3000
# Production'da doğrudan backend URL'i ver (örn. https://api.digicatalog.com)
VITE_API_URL=/api

# App adı (header/title)
VITE_APP_NAME=DijiCatalog
